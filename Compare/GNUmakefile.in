include $(GNUSTEP_MAKEFILES)/common.make

VERSION = 0.1
PACKAGE_NAME = Compare
BUNDLE_NAME = Compare
Compare_PRINCIPAL_CLASS = Compare
BUNDLE_EXTENSION = .bundle
BUNDLE_INSTALL_DIR = $(GNUSTEP_BUNDLES)/Triva

#
# Additional libraries
#

ifeq ($(FOUNDATION_LIB), apple)
  LDFLAGS += -F../Triva -framework Triva -framework General
else
  TimeSliceAggregation_BUNDLE_LIBS += -lTriva -lGeneral
  ADDITIONAL_LIB_DIRS += -L../Triva/Triva.framework/Versions/Current
endif

#
# Resource files
#
Compare_RESOURCE_FILES = \
Resources/Compare.gorm \


#
# Header files
#
Compare_HEADER_FILES = \
Timeline.h \
Marker.h \
Compare.h \
CompareView.h \
CompareController.h

#
# Class files
#
Compare_OBJC_FILES = \
Timeline.m \
Marker.m \
Compare.m \
CompareView.m \
CompareController.m \
CompareController+TypeHierarchy.m \
CompareController+Marker.m

Compare_INCLUDE_DIRS += -I../ -I./

#
# Makefiles
#
-include GNUmakefile.preamble
include $(GNUSTEP_MAKEFILES)/aggregate.make
include $(GNUSTEP_MAKEFILES)/bundle.make
-include GNUmakefile.postamble
