include $(GNUSTEP_MAKEFILES)/common.make

CC=$(CXX)

PACKAGE_NAME=GraphView
BUNDLE_NAME=GraphView
BUNDLE_EXTENSION=.bundle
BUNDLE_INSTALL_DIR=$(GNUSTEP_BUNDLES)/Triva
GraphView_PRINCIPAL_CLASS=GraphView

GraphView_OBJC_FILES=
GraphView_OBJCC_FILES = \
	Graph.mm \
	GraphWindow.mm \
	GraphWindowAuto.mm \
	GraphDraw.mm

GraphView_INCLUDE_DIRS += @CPPFLAGS@
GraphView_LIB_DIRS += @LIBS@
GraphView_LDFLAGS += @LDFLAGS@

include $(GNUSTEP_MAKEFILES)/bundle.make

after-distclean::
after-all::
after-clean:: 
