#GNUSTEP_INSTALLATION_DOMAIN = USER
#
include $(GNUSTEP_MAKEFILES)/common.make

CC=$(CXX)

SUBPROJECT_NAME=camera
camera_OBJC_FILES=
camera_OBJCC_FILES=CameraManager.mm Camera2Manager.mm

camera_INCLUDE_DIRS += @CPPFLAGS@ 
camera_LIB_DIRS += @LIBS@ 
camera_LDFLAGS += @LDFLAGS@

include $(GNUSTEP_MAKEFILES)/subproject.make

after-distclean::
after-all::
after-clean:: 
