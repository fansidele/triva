#GNUSTEP_INSTALLATION_DOMAIN = USER
#
include $(GNUSTEP_MAKEFILES)/common.make

CC=$(CXX)

SUBPROJECT_NAME=selector
selector_OBJC_FILES=
selector_OBJCC_FILES=SelectorManager.mm

selector_INCLUDE_DIRS += @CPPFLAGS@
selector_LIB_DIRS += @LIBS@
selector_LDFLAGS += @LDFLAGS@

include $(GNUSTEP_MAKEFILES)/subproject.make

after-distclean::
after-all::
after-clean:: 
