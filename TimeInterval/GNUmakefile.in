include $(GNUSTEP_MAKEFILES)/common.make

VERSION = 0.1
PACKAGE_NAME = TimeInterval
BUNDLE_NAME = TimeInterval
TimeInterval_PRINCIPAL_CLASS = TimeInterval
BUNDLE_EXTENSION = .bundle
BUNDLE_INSTALL_DIR = $(GNUSTEP_BUNDLES)/Triva

ifeq ($(FOUNDATION_LIB), apple)
  LDFLAGS += -F../Triva -framework Triva -framework General
  ADDITIONAL_INCLUDE_DIRS += -framework Renaissance
  ADDITIONAL_GUI_LIBS += -framework Renaissance
else
  ADDITIONAL_LIB_DIRS += -L../Triva/Triva.framework/Versions/Current
  ADDITIONAL_GUI_LIBS += -lRenaissance
endif

TimeInterval_RESOURCE_FILES = \
	TimeInterval.gsmarkup \
	TimeInterval.plist
TimeInterval_HEADER_FILES = TimeInterval.h SliceView.h
TimeInterval_OBJC_FILES = TimeInterval.m SliceView.m
TimeInterval_INCLUDE_DIRS += -I../ -I./ @CPPFLAGS@
TimeInterval_LDFLAGS += @LDFLAGS@

-include GNUmakefile.preamble
include $(GNUSTEP_MAKEFILES)/aggregate.make
include $(GNUSTEP_MAKEFILES)/bundle.make
-include GNUmakefile.postamble
